import { MeshProvider } from '@meshsdk/react';
import { CardanoWallet, MeshBadge, useWallet } from "@meshsdk/react";

// <MeshProvider>
// <CardanoWallet />
// </MeshProvider>

export default function MyCardanoComponent() {

    //const { connected } = useWallet();

    return (
        <>
            <p>MyCardanoComponent</p>

        </>
    );
}